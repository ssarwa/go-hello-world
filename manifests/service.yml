apiVersion: v1
kind: Service
metadata:
    name: "azurecluster-327d"
    labels:
        app: "azurecluster-327d"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "azurecluster-327d"